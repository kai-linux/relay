# Required: OpenAI API key (used for Whisper STT and TTS)
OPENAI_API_KEY=sk-...

# Optional: STT/TTS model settings
# RELAY_STT_MODEL=whisper-1
# RELAY_TTS_MODEL=tts-1
# RELAY_TTS_VOICE=nova

# Agent backend: "claude-code" or "codex"
RELAY_AGENT_BACKEND=claude-code

# Skip permission prompts (claude: --dangerously-skip-permissions, codex: full-auto)
RELAY_SKIP_PERMISSIONS=true

# Working directory for the agent (default: ~)
# RELAY_WORK_DIR=/home/you/projects
# RELAY_AGENT_TIMEOUT=300

# Optional: server settings
# RELAY_HOST=0.0.0.0
# RELAY_PORT=5000
# RELAY_SECRET_KEY=change-me-in-production
